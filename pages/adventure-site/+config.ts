import type { Config } from 'vike/types';

export default {
    title: 'Adventure site generator | Mausritter',
    description: `Generate random adventure sites for Mausritter.`,
} satisfies Config;
